import React from "react"
import Button from "../../elements/Button/Button"

const WordOptionsButton = () => {
  return (
    <Button title="Open Word Options" style="small" onClick={event => {}} />
  )
}
export default WordOptionsButton
